/// גרסת המתאם. חייבת להיות זהה ל-`version` ב-`pubspec.yaml` — נאכף
/// ב-`test/version_test.dart`, כדי שהתוסף יראה ב-`/hello` את הגרסה
/// האמיתית של הקובץ שרץ.
const String companionVersion = '5.1.0';
